<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SafeNotes</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    


<div class="bg-gray-100 flex items-center justify-center min-h-screen">

    <div class="bg-white shadow-lg rounded-2xl p-8 w-96">
        <h2 class="text-2xl font-bold text-center text-gray-700 mb-6">Login</h2>

        <form action="/login" method="post" class="space-y-4">
            
            <!-- Email -->
            <div>
                <input type="email" name="email" placeholder="Enter Email"
                    class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                    required>
            </div>
        
            <!-- Password -->
            <div>
                <input type="password" name="password" placeholder="Password"
                    class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
                    required>
            </div>
        
            <!-- Error message -->
            <% if (error) { %>
                <p class="text-red-500 text-sm text-center"><%= error %></p>
            <% } %>
        
            <!-- Submit -->
            <div class="flex items-center justify-between">
                <div class="flex items-center">
                    <input type="checkbox" id="remember" class="w-4 h-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded">
                    <label for="remember" class="ml-2 text-sm text-gray-600">Remember me</label>
                </div>
        
                <input type="submit" value="Login"
                    class="bg-blue-500 text-white px-6 py-2 rounded-lg hover:bg-blue-600 cursor-pointer">
            </div>
        
            <!-- Signup -->
            <p class="text-sm text-center text-gray-600">New User?
                <a href="/signup" class="text-blue-500 hover:underline">Signup</a>
            </p>
        </form>
        
    </div>

</div>
</body>

</html>
